#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	int a,b;
	a=0;
	
	for(a=0;b>=0,b<100;a++){
	printf("b=");
	scanf("%d",&b);}
	printf("so lan nhap = %d",a);
	return 0;
}
